#include <bits/stdc++.h>

using namespace std;
int a, b, c, d, e, f;

int main() {
    cin.tie(0); cin.sync_with_stdio(0);
    cin >> a >> b >> c >> d >> e >> f;
    cout << d + e + f << " " << a*d + b*e + c*f << "\n";
    return 0;
}